{
  "name": "game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "getbundle": "mv dist/*.js ../public/",
    "cleanbundle": "rm -rf ../public/*.js",
    "build": "npm run cleanbundle && tsc && vite build && npm run getbundle",
    "preview": "vite preview"
  },
  "devDependencies": {
    "dompurify": "^3.2.6",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "htmx.org": "^1.9.12",
    "pixi.js": "^8.9.1"
  }
}
