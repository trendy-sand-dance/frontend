<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Collapsible Sidebar</title>
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>


<body class="flex h-screen">

  <!-- Sidebar will be swapped here -->
  <div id="sidebar" class="transition-all duration-300">
    <%- include('partials/sidebar', { isCollapsed }) %>
  </div>

  <!-- Main content -->
  <div class="flex-1">
    <header class="bg-gray-100 p-4 flex justify-between">
      <h1 class="text-xl font-bold">HTMX Sidebar Example</h1>
      <button 
        hx-get="/toggle-sidebar" 
        hx-target="#sidebar" 
        hx-swap="outerHTML"
        class="bg-blue-500 text-white px-4 py-2 rounded"
      >
        Toggle Sidebar
      </button>
    </header>

    <main class="p-4">
      <p>This is your game content!</p>
    </main>
  </div>
</body>
</html>
