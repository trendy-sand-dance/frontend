<div id="sidebar" class="bg-white border-r transition-all duration-300 ease-in-out overflow-hidden  h-full <%= isCollapsed ? 'w-16' : 'w-64' %> p-4">
	<% if (isCollapsed) { %>
	  <div class="text-xl font-bold text-center">🐸</div>
	<% } else { %>
	  <div class="text-xl font-bold">expanded :D</div>
	  <nav class="mt-4 space-y-2">
		<a href="#" class="block px-2 py-1 hover:bg-gray-100">🏠 Home</a>
		<a href="#" class="block px-2 py-1 hover:bg-gray-100">🎮 Game</a>
	  </nav>
	<% } %>
  </div>
  